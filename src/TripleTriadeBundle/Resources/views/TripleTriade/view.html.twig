{# src/TripleTriade/Resources/views/TripleTriade/view.html.twig #}

{% extends "TripleTriadeBundle::layout.html.twig" %}

{% block title %}
  Lecture d'une carte - {{ parent() }}
{% endblock %}

{% block triple_triade_body %}
  <h2>{{ card.title }}</h2>
  <i>Par {{ card.author }}, le {{ card.date|date('d/m/Y') }}</i>

  <div class="well">
    {{ card.content }}
  </div>

  <p>
    <a href="{{ path('triple_triade_homepage') }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour à la liste
    </a>
  </p>

{% endblock %}